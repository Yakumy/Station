{
  "name": "yakumy-station",
  "version": "1.0.5",
  "private": true,
  "description": "Station — linked multi-monitor workstations for Omarchy and Hyprland.",
  "type": "module",
  "scripts": {
    "build": "bun build --compile --minify --bytecode index.js --outfile bin/station",
    "build:check": "bun run build && test -x bin/station",
    "start": "bun run index.js"
  }
}
